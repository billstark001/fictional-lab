import { PageContext } from "vike/types";


// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function onBeforeRenderHtml(pageContext: PageContext) {
  // const pageConfig = pageContext.source.Page;
  // const componentPath: string = (pageConfig as any).definedAt;
  // console.log(componentPath, import.meta.hot);
}